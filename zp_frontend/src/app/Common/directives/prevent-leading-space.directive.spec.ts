import { PreventLeadingSpaceDirective } from './prevent-leading-space.directive';

describe('PreventLeadingSpaceDirective', () => {
  it('should create an instance', () => {
    const directive = new PreventLeadingSpaceDirective();
    expect(directive).toBeTruthy();
  });
});
